sudo: required
services:
  - docker

before_install:
  - docker build -t marshallrhodesfox/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true marshallrhodesfox/docker-react npm run test
